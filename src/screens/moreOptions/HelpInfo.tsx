import {Text, View} from 'react-native';
import React from 'react';

const HelpInfo = () => {
  return (
    <View style={{flex: 1}}>
      <Text>HelpInfo</Text>
    </View>
  );
};

export default HelpInfo;
